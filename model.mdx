---
title: 'Genie Models'
description: 'The models used by Genie and how to switch between them.'
---

So Genie uses two engines for it's AI capabilities. **Gemini** and **GPT** engines.

## Switching Models

You can switch between the two engines using the `switch` command. Here's how you can do it:

```bash
genie switch
```

Read more about the `switch` command [here](/commands/switch).

## Current Configuration

You can check the current model and the current engine being used by Genie using the `engine` command. Here's how you can do it:

```bash
genie engine
```

Read more about the `engine` command [here](/commands/engine). 