---
title: 'verify'
description: 'Reference for the `verify` command in Genie, the AI-powered CLI companion.'
---

The `verify` command is used to verify your support status for Genie. This command will confirm the status of your payment and provide your device with access to supporter status.

## Usage

```bash
genie verify "me@domain.com"
```

Replace `me@domain.com` with the email address you used to become a supporter. You'll receive a confirmation link in your email to verify your account. If you're a supporter, you'll receive a success message in the terminal.

There are no additional arguments required for this command. 